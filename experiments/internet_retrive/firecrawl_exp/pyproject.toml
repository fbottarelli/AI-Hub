[project]
name = "firecrawl-exp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "firecrawl-py>=1.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
